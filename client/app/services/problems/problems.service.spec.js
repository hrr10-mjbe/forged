'use strict';

describe('Service: Problems', function () {

  // load the service's module
  beforeEach(module('hrr10MjbeApp'));

  // instantiate service
  var problems;
  //beforeEach(inject(function (_problems_) {
    //problems = _problems_;
  //}));

  it('should do something', function () {
    //expect(!!problems).to.be.true;
  });

});
